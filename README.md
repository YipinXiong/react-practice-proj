#This is my personal React practice project.

The api config file has been hide in `.gitignore`. Because the api and the data I am using is private to my current employer. 

So you cannot run it directly
